import React from 'react';

// No example prompts - clean, minimal start
export function ExamplePrompts(sendMessage?: { (event: React.UIEvent, messageInput?: string): void | undefined }) {
  return null;
}
